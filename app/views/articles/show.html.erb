<!--Nav Bar-->
<header class="navbar">
  <section class="navbar-section">
    <%= link_to 'Home', {controller: 'welcome'}, class: "btn btn-link"%>
    <%= link_to 'All Posts', {controller: 'articles'}, class: "btn btn-link"%>
  </section>
  <section class="navbar-center">
    <!-- centered logo or brand -->
  </section>
  <section class="navbar-section">
    <a href="twitter.com/tristanhari" class="btn btn-link">Twitter</a>
    <a href="github.com/kptristan24" class="btn btn-link">GitHub</a>
  </section>
</header>

<!--Post Title-->
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= raw @article.text %>
</p>

<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
